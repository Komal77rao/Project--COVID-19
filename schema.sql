CREATE TABLE vaccineData (
	ID INT PRIMARY KEY UNIQUE,
	AGE DECIMAL,
	MANUFACTURER VARCHAR,
	DOSE VARCHAR,
	VACCINE VARCHAR,
	VAX_TYPE VARCHAR,
	VAX_LOT VARCHAR,
	VAX_SITE VARCHAR,
	SYMPTOM1 VARCHAR,
	SYMPTOM2 VARCHAR,	 
	SYMPTOM3 VARCHAR,
	STATE VARCHAR,
	SEX VARCHAR,
	DIED VARCHAR,
	HOSPITAL VARCHAR,
	RECOVD VARCHAR,
	VAX_DATE DATE,
	ONSET_DATE DATE,
	NUMDAYS DECIMAL,
	V_ADMINBY VARCHAR,
    OTHER_MEDS VARCHAR,
	CUR_ILL VARCHAR,
	HISTORY VARCHAR,
	FORM_VERS INT,
	TODAYS_DATE DATE,
	ALLERGIES VARCHAR
);